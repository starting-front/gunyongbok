export interface Profile {
  name: string;
  email: string;
  tel: string;
  // field: string;
  // job: string;
  introduce: string;
  myKeywords: string[] | [];
}

export interface Overview {
  backgroud: string;
  objective: string;
  role: string;
}
